# Root .env.example — reference for all components.
# Copy component-specific .env.example files for actual use:
#   cp bridge-service/.env.example bridge-service/.env
#   cp mcp-demo-server/.env.example mcp-demo-server/.env

# See each component's .env.example for detailed configuration options.
